interface:
  display_name: "Atlas"
  short_description: "Repo-specific patterns and workflows for Atlas"
  default_prompt: "Use the Atlas repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true